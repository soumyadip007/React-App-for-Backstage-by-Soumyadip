{
  "name": "${{values.spa_name}}",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "source .env && spaship deploy --apikey=$SPASHIP_API_KEY -e=$SPASHIP_DEPLOYMENT_URL -r=$SPASHIP_REF --builddir=$SPASHIP_BUILD_DIR"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.0.26",
    "@types/react-dom": "^18.0.9",
    "@vitejs/plugin-react": "^3.0.0",
    "@spaship/cli": "^1.5.5",
    "vite": "^4.0.0"
  }
}